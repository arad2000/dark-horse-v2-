شکست کش — v50
==============
docs جایگزین:
  index.html
  shell.js
  shell.css
  sw.js

بعد از آپلود روی گوشی:
1) سایت را کامل ببند
2) اگر PWA نصب است: Clear storage همان سایت یا حذف و نصب مجدد
3) یا Chrome → Site settings → Clear & reset

SW الان network-first برای shell است.
