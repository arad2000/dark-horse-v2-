# Payment callback fix

Sandbox success now uses top-level navigation to the Liara callback so the server can verify and grant credits before returning to the frontend. Normal billing policy remains 1 free test and paid packs thereafter.