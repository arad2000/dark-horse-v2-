# CORS hardening

Production API CORS is restricted to the canonical frontend origin:

- `https://asbe-siah.ir`

This branch intentionally does not change the production deployment branch or runtime cutover state.